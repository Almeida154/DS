public class Lampada {
	//Atributos
	private boolean status;
	
	//Comportamentos
	public boolean getStatus() {
		return this.status;
	}

	public void setStatus(boolean stts) {
		this.status = stts;
	}

	public void ligarLampada() {
		if(!getStatus()) setStatus(true);
		else System.out.println("--JÁ ESTÁ ACESSA--");
		this.observar();
	}
	
	public void desligarLampada() {
		if(getStatus()) setStatus(false);
		else System.out.println("--JÁ ESTÁ DESLIGADA--");
		this.observar();
	}
	
	public void observar() {
		if(getStatus()) System.out.println("--ACESSA!--");
		else System.out.println("--DESLIGADA!--");
	}
}